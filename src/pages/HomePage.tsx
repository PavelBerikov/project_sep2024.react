import  {FC} from 'react';
import Menu from "../components/menu/Menu.tsx";

const HomePage:FC = () => {
    return (
        <div>
            <Menu/>
        </div>
    );
};

export default HomePage;